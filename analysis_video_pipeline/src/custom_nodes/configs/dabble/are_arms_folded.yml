# Custom dabble node for checking if the arms of a pose are folded

# It takes in tracking IDs and keypoints from model.posenet
input: ['img', 'keypoints', 'obj_attrs']

# It outputs the total number of frames in which the arms are folded
# The output is categorised by tracking IDs
output: ['arms_folded']